package immersive_furniture.client.gui;

public class VehicleScreenRegistry {
    /*
    public static void openScreen(InventoryVehicleEntity vehicle, LocalPlayer player, ArtisansWorkstationScreen message) {
        Minecraft client = Minecraft.getInstance();
        if (client.level != null && client.player != null) {
            ArtisansWorkstationScreenHandler handler = (ArtisansWorkstationScreenHandler) vehicle.createMenu(message.getSyncId(), client.player.getInventory(), client.player);
            ArtisansWorkstationScreen screen = new ArtisansWorkstationScreen(handler, client.player.getInventory(), vehicle.getDisplayName());
            client.player.containerMenu = screen.getMenu();
            client.setScreen(screen);
        }
    };
    */
}
